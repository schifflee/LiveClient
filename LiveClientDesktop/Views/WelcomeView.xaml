<UserControl x:Class="LiveClientDesktop.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="欢迎:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="{Binding TeachersNames}"   VerticalAlignment="Center"/>
        <TextBlock Grid.Column="2" Text=",课堂主题:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="3" Text="{Binding Title}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
